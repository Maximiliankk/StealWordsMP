
next version goal:
-playable
  -sync player names
  -stop same word steals
  -winscreen/celebration for winner

future features:
-able to restart games without restarting server every time
-test on Mac OSX
-basic networked lobby
  -list of games currently running (can join/leave any at any time)
  -set your name
    -if player has the same name dont let them into the game (or just let people have same name)
-allow changing WINDOW_WIDTH and WINDOW_HEIGHT

    
cleanup:
-break up server and client code into 2 files
-warnings

tasks:
-set Rendering code macro with Sever macro


done:
  -force players to name themselves when starting the app
